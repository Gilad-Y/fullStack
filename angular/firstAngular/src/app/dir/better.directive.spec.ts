import { BetterDirective } from './better.directive';

describe('BetterDirective', () => {
  it('should create an instance', () => {
    const directive = new BetterDirective();
    expect(directive).toBeTruthy();
  });
});
